# Image Pairs (WordPress Plugin)

Плагин для WordPress, который выводит **пары изображений** с подписями и простым лайтбоксом. Управление выводом — через шорткод `[image_pairs]` с гибкими параметрами (темы, сортировка, перемешивание, размер изображений и отключение подписей).

> Версия плагина: **1.8.0** (см. заголовок плагина в файле).

---

## 🚀 Возможности

- Кастомный тип записей **`image_pair`** (в админке «Пары изображений»).
- Для каждой записи: **Картинка 1**, **Картинка 2**, **подпись**.
- Таксономия **`ip_topic`** («Темы») с иерархией.
- Вывод на фронтенде через шорткод **`[image_pairs]`**.
- Фильтрация по темам (`topic`, `topic_ids`) и логика отбора (`operator`).
- Сортировка (`orderby`, `order`) и **перемешивание** (`shuffle`).
- **Отключение подписей** под парами (`captions=0`).
- Адаптивная сетка **2×2** (на мобильных тоже две колонки, как по ТЗ).
- Лёгкий лайтбокс на чистом JS: стрелки, `Esc`, кликом по фону, кнопка ✕.
- Стили/скрипты лайтбокса подключаются **только когда используется шорткод** (через `wp_footer`).

---

## 📦 Установка

1. Скопируйте исходный файл плагина (например, `image-pairs.php`) в папку:
   `wp-content/plugins/image-pairs/`
2. Активируйте плагин в **Админка → Плагины**.
3. В меню появится раздел **Пары изображений** (тип `image_pair`).

**Требования:** WordPress 5.0+ (Gutenberg/REST совместим), PHP 7.2+.

---

## 🧩 Как пользоваться

1. В админке откройте **Пары изображений → Добавить**.
2. Выберите **Картинку 1** и **Картинку 2** через медиаменеджер.
3. (Необязательно) заполните **подпись** — она появится под парой.
4. (Необязательно) назначьте **Темы** (таксономия `ip_topic`).
5. Сохраните запись и вставьте шорткод на страницу/запись:

```php
[image_pairs]
```

---

## ⚙️ Параметры шорткода

Базовая форма:
```php
[image_pairs]
```

| Параметр     | Назначение | По умолчанию | Допустимые значения / заметки | Пример |
|--------------|------------|--------------|--------------------------------|--------|
| `orderby`    | Поле сортировки | `date` | Любое поддерживаемое WP: `date`, `title`, `menu_order`, `ID`, … | `[image_pairs orderby="title"]` |
| `order`      | Направление сортировки | `DESC` | `ASC` / `DESC` | `[image_pairs order="ASC"]` |
| `size`       | Размер изображений | `large` | Любой зарегистрированный размер (`thumbnail`, `medium`, `large`, `full`, кастомные) | `[image_pairs size="medium"]` |
| `topic`      | Фильтр по **slug** тем | — | Один или несколько slug через запятую | `[image_pairs topic="rekonstrukciya,zavod"]` |
| `topic_ids`  | Фильтр по **ID** тем | — | Список ID через запятую | `[image_pairs topic_ids="12,34"]` |
| `operator`   | Логика фильтра по темам | `IN` | `IN`, `AND`, `NOT IN` | `[image_pairs topic="a,b" operator="AND"]` |
| `shuffle`    | Перемешать пары после выборки | `0` | Истина: `1`, `true`, `yes`, `on` | `[image_pairs shuffle="1"]` |
| `captions`   | Отображать подписи под парами | `1` | Ложь: `0`, `false`, `no`, `off` | `[image_pairs captions="0"]` |

> Параметры безопасно нормализуются (санитизация ключей `orderby`, проверка `operator`, приведение флагов к булевым).

---

## 💡 Примеры

**Все пары, перемешать, без подписей:**
```php
[image_pairs shuffle="1" captions="0"]
```

**Фильтр по теме (slug):**
```php
[image_pairs topic="restavraciya"]
```

**Несколько тем и логика «И»:**
```php
[image_pairs topic="rekonstrukciya,zavod" operator="AND"]`
```

**Кастомный размер изображений:**
```php
[image_pairs size="medium_large"]
```

---

## 🔧 Как это работает (из кода)

- Регистрируются:
  - **CPT**: `image_pair` (поддерживает только `title`, `show_in_rest` включён).
  - **Таксономия**: `ip_topic` (иерархическая, `show_ui`, `show_in_rest`).
- В метабоксе задаются два вложения (ID) и подпись. Данные сохраняются в метаполя: `_ip_img1`, `_ip_img2`, `_ip_caption`.
- Шорткод `[image_pairs]` строит `WP_Query` по CPT, применяя `tax_query`, сортировку и постобработку (`shuffle()` по списку постов при необходимости).
- Каждая пара рендерится как две ссылки-изображения + (опционально) подпись.
  - `alt` берётся из `_wp_attachment_image_alt`, при отсутствии — из заголовка вложения.
  - Изображения используют `loading="lazy"` и `decoding="async"`.
- Лайтбокс (CSS/JS) добавляется в `wp_footer` **только если на странице был шорткод** (флаг `$GLOBALS['ip_enqueue_lightbox']`).
  - Навигация стрелками (`←/→`), закрытие по `Esc` и клику по фону/кнопке ✕.
  - Кнопки «вперёд/назад» показываются только при >1 изображении в группе.
  - Во время открытия лайтбокса скрываются контейнеры популярных сторонних лайтбоксов, чтобы избежать конфликтов.

---

## ✅ Accessibility & UX

- Лайтбокс помечен как `role="dialog"` и `aria-modal="true"`.
- Управление с клавиатуры: `Esc` для закрытия, `←/→` для навигации.
- Группировка изображений в рамках одного шорткода — через уникальный `data-ipbox` (без смешивания с другими галереями на странице).

---

## 🧪 Советы по интеграции

- Для аккуратных `alt`-тегов задавайте **Альтернативный текст** у вложений в медиабиблиотеке.
- Если включены сторонние лайтбоксы (Elementor, Fancybox и т. п.), конфликтов не будет: во время просмотра нашего лайтбокса они принудительно скрываются.
- Если подписи не нужны глобально, просто добавляйте `captions="0"` в шорткод.

---

## 🐞 Траблшутинг

- **Ничего не выводится:** убедитесь, что есть опубликованные записи типа `image_pair` и хотя бы одно изображение в каждой.
- **Проблемы с размерами:** проверьте, что используемый размер (`size`) зарегистрирован темой/плагином.
- **Стили «ломаются»:** проверьте кастомные темы/билдеры. В плагине используются нейтральные классы (`.ip-wrap`, `.ip-row`, `.ip-caption`).

---

## 📜 Changelog

### 1.8.0
- Добавлен параметр шорткода **`captions`** для отключения подписей (`captions="0"`).
- Улучшено: группы лайтбокса изолируются по уникальному `data-ipbox` для каждого инстанса шорткода.
- Мелкие улучшения типографики и адаптива.

---

## 📝 Лицензия

**GPL-2.0+** — свободно используйте и модифицируйте.

---

## 👤 Автор

**akent4000**  
Плагин «Image Pairs». Удобный способ показать сравнения «до/после», «вариант 1/2» и любые парные визуальные материалы.
